generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Setting {
  id     String  @id @default(cuid())
  userId String? // null = global
  key    String
  value  String

  @@unique([userId, key])
}
